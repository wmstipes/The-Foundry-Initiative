name: Service Pulse CI

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/service-pulse/**"
      - ".github/workflows/service-pulse-ci.yml"
  push:
    branches: [main]
    paths:
      - "apps/service-pulse/**"
      - ".github/workflows/service-pulse-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - run: python -m unittest discover -s apps/service-pulse/tests -v
      - run: python -m py_compile apps/service-pulse/main.py
      - run: docker build --tag service-pulse:candidate apps/service-pulse
