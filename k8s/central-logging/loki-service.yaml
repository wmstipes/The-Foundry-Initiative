apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: forge-observability
  labels:
    app: loki
    project: signalforge
spec:
  type: ClusterIP
  selector:
    app: loki
  ports:
  - name: http
    port: 3100
    targetPort: http
