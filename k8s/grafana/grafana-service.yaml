apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    app: grafana
    project: signalforge
  namespace: forge-observability
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
  - name: http
    port: 3000
    targetPort: http
