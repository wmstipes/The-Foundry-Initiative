apiVersion: v1
kind: Service
metadata:
  name: service-pulse-probe
  namespace: forge-pulse
  labels:
    app: service-pulse-probe
    project: signalforge
    app.kubernetes.io/name: service-pulse-probe
    app.kubernetes.io/part-of: signalforge
spec:
  type: ClusterIP
  selector:
    app: service-pulse-probe
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
