apiVersion: v1
kind: Service
metadata:
  name: restaurant-api
  namespace: forge-restaurant
  labels:
    app: restaurant-api
    project: signalforge
spec:
  type: ClusterIP
  selector:
    app: restaurant-api
  ports:
  - name: http
    port: 80
    targetPort: http
